# Portable core

This directory is reserved for the dependency-free portable Hexpira codec.
The module boundaries are defined in
[`../../specification/ARCHITECTURE.md`](../../specification/ARCHITECTURE.md).

Until the single-file reference implementation is split and independently
tested, `reference/web/index.html` remains the executable reference. Do not add
empty API stubs here: each future module should arrive with conformance tests.


